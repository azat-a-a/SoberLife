// Copy to Secrets.xcconfig (add Secrets.xcconfig to .gitignore locally) and assign the file under
// Xcode → Project → Info → Configurations for Debug and Release.

SUPABASE_URL = REPLACE_WITH_SUPABASE_URL
SUPABASE_ANON_KEY = REPLACE_WITH_ANON_KEY
